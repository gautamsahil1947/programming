#include <stdio.h>
int main (void)
{
	int n;
	printf("enter the number: ");
	scanf("%d", &n);
	// printing the integer n
	printf("the number is %d\n", n);
	return 0;
}
// question: what is the difference between %i and %d in c ?
